YUI.add("<%= this.module %>", function(Y) {
    "use strict";
    Y.namespace("Falco.Templates")["<%= this.template %>"] = Y.Template.Micro.revive(<%== this.fn %>);
}, "@VERSION@", {
    requires : [
        "template"
    ]
});
