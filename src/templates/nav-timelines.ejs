<% data.timelines.base.forEach(function(timeline) { %>
    <%== data._t.timeline({ timeline : timeline, path : data.path }) %>
<% }) %>

<% data.timelines.list.forEach(function(timeline) { %>
    <%== data._t.timeline({ timeline : timeline, path : data.path }) %>
<% }) %>

<% data.timelines.search.forEach(function(timeline) { %>
    <%== data._t.timeline({ timeline : timeline, path : data.path }) %>
<% }) %>
